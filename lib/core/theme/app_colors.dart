import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // =========================================================
  // PRIMARY / HEADING
  // FIRST FILE PURPLE
  // =========================================================

  /// Main heading / brand color
  static const Color primary = Color(0xFF5B1A8E);

  /// Light purple
  static const Color primaryLight = Color(0xFFF7EEFC);

  /// Dark purple
  static const Color primaryDark = Color(0xFF2A063F);

  // =========================================================
  // SECONDARY / MAROON THEME
  // SECOND FILE COLORS
  // =========================================================

  static const Color maroon = Color(0xFF8A1538);

  static const Color maroonLight = Color(0xFFFAF0F2);

  static const Color maroonDark = Color(0xFF5C0B24);

  // =========================================================
  // CATEGORY & CARD COLORS
  // =========================================================

  static const Color categoryCard = Color(0xFF4A2C1D);

  static const Color categoryCardSecondary = Color(0xFF331C11);

  // =========================================================
  // LIGHT THEME
  // =========================================================

  static const Color background = Color(0xFFF9F8F8);

  static const Color surface = Color(0xFFFFFFFF);

  static const Color surfaceSoft = Color(0xFFFFF4F6);

  static const Color cream = Color(0xFFFFFAF7);

  // =========================================================
  // DARK THEME
  // =========================================================

  static const Color darkBackground = Color(0xFF24151B);

  static const Color darkSurface = Color(0xFF321D26);

  static const Color darkSurfaceSecondary = Color(0xFF3D2530);

  static const Color darkSurfaceElevated = Color(0xFF49303A);

  static const Color darkBorder = Color(0xFF51333D);

  static const Color darkDivider = Color(0xFF422832);

  // =========================================================
  // TEXT COLORS - LIGHT
  // =========================================================

  static const Color textPrimary = Color(0xFF1E1E1E);

  static const Color textSecondary = Color(0xFF666666);

  static const Color textHint = Color(0xFF9E9E9E);

  static const Color textDisabled = Color(0xFFBDBDBD);

  static const Color textWhite = Color(0xFFFFFFFF);

  // First file purple text
  static const Color textPurple = Color(0xFF5B1A8E);

  // Second file maroon accent text
  static const Color textMaroon = Color(0xFF8A1538);

  // =========================================================
  // TEXT COLORS - DARK
  // =========================================================

  static const Color darkTextPrimary = Color(0xFFF8F1F3);

  static const Color darkTextSecondary = Color(0xFFC9B8BF);

  static const Color darkTextHint = Color(0xFF9E8B92);

  static const Color darkTextDisabled = Color(0xFF705963);

  // =========================================================
  // CATEGORY
  // =========================================================

  static const Color categoryIconBackground = Color(0xFFFFF0DD);

  static const Color categoryIconColor = Color(0xFFFF8A00);

  // =========================================================
  // INPUT / SEARCH
  // =========================================================

  static const Color inputBackground = Color(0xFFF4F4F4);

  static const Color inputBackgroundDark = Color(0xFF3A222B);

  static const Color searchBackground = Color(0xFFFFFFFF);

  static const Color searchBackgroundDark = Color(0xFF3A222B);

  static const Color searchIcon = Color(0xFF8A1538);

  // =========================================================
  // BORDERS
  // =========================================================

  static const Color border = Color(0xFFE0E0E0);

  static const Color borderLight = Color(0xFFEDE7E9);

  // Purple heading focused border
  static const Color borderFocused = Color(0xFF5B1A8E);

  // Maroon accent border
  static const Color borderAccent = Color(0xFF8A1538);

  static const Color divider = Color(0xFFE0E0E0);

  // =========================================================
  // CHIPS / FILTERS
  // =========================================================

  static const Color chipBackground = Color(0xFFF0F0F0);

  static const Color chipSelected = Color(0xFF5B1A8E);

  static const Color chipText = Color(0xFF5B1A8E);

  static const Color chipSelectedText = Color(0xFFFFFFFF);

  static const Color chipOrange = Color(0xFFFFF0DC);

  static const Color chipOrangeText = Color(0xFFE66F00);

  // =========================================================
  // ACCENT
  // FIRST FILE ACCENT
  // =========================================================

  static const Color accent = Color(0xFFFF8A00);

  static const Color accentLight = Color(0xFFFFA51F);

  static const Color accentDark = Color(0xFFE66F00);

  static const Color gold = Color(0xFFFFC107);

  static const Color ratingStar = Color(0xFFFFB800);

  // =========================================================
  // STATUS
  // =========================================================

  static const Color success = Color(0xFF2E7D32);

  static const Color successBackground = Color(0xFFE8F5E9);

  static const Color error = Color(0xFFD32F2F);

  static const Color errorBackground = Color(0xFFFFEBEE);

  static const Color warning = Color(0xFFF9A825);

  static const Color warningBackground = Color(0xFFFFF8E1);

  static const Color info = Color(0xFF6C4AB6);

  static const Color infoBackground = Color(0xFFF1EBFA);

  // =========================================================
  // FAVORITE
  // =========================================================

  static const Color favorite = Color(0xFFFF5A5F);

  static const Color favoriteBackground = Color(0xFFFFECEC);

  // =========================================================
  // OVERLAY / GLASS
  // =========================================================

  static const Color overlay = Color(0x66000000);

  static const Color darkOverlay = Color(0x99000000);

  static const Color glassWhite = Color(0x26FFFFFF);

  static const Color glassPurple = Color(0x335B1A8E);

  static const Color glassMaroon = Color(0x338A1538);

  static const Color glassOrange = Color(0x33FF8A00);

  // =========================================================
  // SHADOW
  // =========================================================

  static const Color shadow = Color(0x0F000000);

  static const Color purpleShadow = Color(0x265B1A8E);

  static const Color maroonShadow = Color(0x268A1538);

  static const Color orangeShadow = Color(0x26FF8A00);

  // =========================================================
  // COMMON
  // =========================================================

  static const Color white = Colors.white;

  static const Color black = Colors.black;

  static const Color transparent = Colors.transparent;
}